



const makeApiCall = async () => {
    let response = await axios(`http https://swapi.dev/api/starships`);
    console.log(response);
    // setCoinData(response.data)

    // make call to SWAPI 
    
    // display some of this data
    //set the state
  }